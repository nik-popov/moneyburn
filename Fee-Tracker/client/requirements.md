## Packages
date-fns | Formatting timestamps (e.g., "MMM d, h:mm a")
framer-motion | Smooth animations for list items and counters
lucide-react | Icons for UI elements

## Notes
- Fees are stored in cents, need formatting util to display as dollars ($10.00).
- Dashboard layout with a "Running Tab" summary.
